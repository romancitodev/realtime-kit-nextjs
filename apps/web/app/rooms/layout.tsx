export const metadata = {
	title: "Socket.io | Chat-rooms",
	description: "Created by @romancitodev",
};

export default function RootLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
